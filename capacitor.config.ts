import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'new-items-added',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
